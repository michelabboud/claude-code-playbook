# Validation — mechanical review of the local layer (0.1.16, held)

- **Review:** [`2026-09-21-local-layer-mechanical-review.md`](2026-09-21-local-layer-mechanical-review.md), committed as returned. Reviewer: Sonnet 5 lane, separate process, read-only worktree at target `1b2ff27`, base `0270322` (published 0.1.15). Verdict: **FAIL**, four blocking findings.
- **Validated by:** the coordinator (Fable 5.1), against source at the pinned target, 2026-09-21.

| # | Severity | Finding | Validation | Ruling |
|---|---|---|---|---|
| B1 | blocking | An unreadable local file (or a local directory without search permission) exits 0, "ok". | **Confirmed by reproduction** at `1b2ff27`: a mode-000 `LOCAL.md` holding a stale entry → `cannot open … Permission denied`, then `ok — 0 dead-words string(s)`, exit 0. | Fix: anything that exists under a local file's name and cannot be read as a regular file is exit 2. |
| B2 | blocking | `INSTALL.md` step M5 copies the migrated local files into place without checking whether one already exists. | **Confirmed** at `INSTALL.md:320-323`; it contradicts the guide's own "never copy over them". | Fix: copy only what does not exist; otherwise stop, show the difference, the user merges. |
| B3 | blocking | `templates/LOCAL.md` ships a live entry: the git-identity Fill with an empty value, outside every fence. | **Confirmed** at `templates/LOCAL.md:112-114`. The ruling that a template ships no live entry was made after this work was built; it applies. | Fix: fenced example; the install guide writes the entry once it has the address; a test finds any unfenced entry line in a template. |
| B4 | blocking | Five mutants that turn a correct exit 1 or 2 into 0 survive the whole suite (unterminated last line; quotation truncated at the separator; a marker after a code span; backslash handling; a swallowed search failure), plus six lower-stakes survivors. | **Confirmed as a test gap, not a code defect:** the unterminated-last-line input gives the correct exit 1 at the target. The brief defines "a test passes while the thing it guards is broken" as blocking. | Fix: one assertion per survivor, each proven against the reviewer's mutant. |
| m1 | minor | Wording tests count per file, not per paragraph; five relocation mutants survive. | Accepted on the reviewer's evidence. | Fix: assert inside the extracted paragraph. |
| m2 | minor | Eleven procedure mutants of `INSTALL.md` and the templates survive. | Accepted. A prose procedure cannot be fully pinned; the load-bearing sentences can. | Fix for: check runs against *staged* text; backup before copy; never replace the directory; never copy a template over a local file; exit 1 stops. |
| m3 | minor | The update never asks before copying; migration never stages the new version; step 4 contradicts itself for a user with a local file; uninstall's restore would overwrite a changed local file; Windows needs `sh`; `ANNOUNCE.md`'s `cp` overwrites. | **Confirmed** by reading the cited lines. | Fix all six. |
| m4 | minor | File names are glob-expanded; a name's meaning depends on the current directory. | Accepted. | Fix: glob characters in a file name are exit 2; `set -f`. |
| m5 | minor → **design** | An Override with no Dead-words line, or a mistyped marker, exits 0. | **Confirmed by reproduction** (`**dead words:**` → exit 0, zero searches). Raised to a decision for both editions: **an Override entry with no valid Dead-words line before the next entry or heading is exit 2.** It resolves every mistyped marker at once, and it is the same principle as the rest of the check: nothing is skipped silently. Recorded in ADR 0004 decision 4. | Fix in both editions; shared multi-line cases are tested in each edition. |
| m6 | minor | Wrong counts (suites, assertions), a stale backlog entry, "five files" where the arithmetic says four. | **Confirmed** (3 + 7 + 4 = 14). The ADR's "five" is the coordinator's miscount; the ADR is unpublished and is corrected in place, marked. | Fix. |
| m7 | minor → **wording of law** | "Never opens them" is untrue: the check script reads both files. | **Confirmed** — the phrase is the coordinator's, in the section-0 paragraph given to the lane verbatim. | Fix everywhere, both editions: "never writes to, copies over or replaces". |
| m8 | minor | BSD `wc -l` pads its output; one assertion would fail on macOS. | Unverified here (no macOS); the fix is harmless. | Fix. |
| informational | — | Read; none needs action beyond the rows above. | — | — |

**Outcome:** every finding confirmed, none dismissed. The batch stays held. Fix round, focused re-check, then the deep review.
