# Cold read — GPT-6 Sol security/data-loss review

- Target: `c0bd8795aaffc4d790025dab76e5d1c5bbd8da27`; base: `5441cd8542fb2ac7c90f7e0c35aea3a1ded8915f`.
- Before this note I read only the scoped diff excluding prior review reports and the candidate `INSTALL.md`. The parent brief disclosed the two prior high-finding themes, so this is an independent source read, not a blind discovery of those themes. No prior report content was read.
- The new source preflight moves Git environment clearing and forced safe configuration into a subshell surrounding all Git calls. It resolves `trust_root` before that subshell, so the caller retains the physical path. The canonical lookup runs from `/` and refuses `/.git`, reducing local repository configuration inheritance. My first question is whether any remaining environment selector or local Git config path can still alter `ls-remote` or object reads.
- The new destination guard checks regular managed files with `find -links +1`. My first question is whether every write path uses this guard immediately before mutation and whether there are filesystem races, unsupported `find` implementations, or hard-linked source/backup paths outside the managed destination set.
- `INSTALL.md` is still executable instruction text loaded from a staged checkout. Authenticating the staged source with code copied from that same file is a bootstrap trust limitation unless the operator obtains the guard from a separately trusted source; I will compare this against the accepted decision before classifying it.
- Copy operations are manual and non-atomic. The text explicitly requires quiescence for uninstall/restore, but install/update copy sequences also have a check-to-use window; I will assess whether this is within the accepted scope or a blocker.
- The changed tests add inherited-`GIT_DIR`, hard-link, and newline regressions. I will reproduce these in isolated scratch fixtures, and inspect the existing restore/uninstall checks for data preservation.
