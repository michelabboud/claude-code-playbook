# This bundle is documentation and reads no environment variables.
#
# The file exists because rule 5.1 says every repository carries one, and
# because the moment this bundle grows a script that reads configuration, the
# place to declare it already exists.
#
# Format when that happens: NAME — purpose, required/optional, safe default.
# Never a real value.
